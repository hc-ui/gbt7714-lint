# Changelog

## 0.3.2 - 2026-08-17

- 剪贴板或文件为空时直接退出并提示，不再当空白参考文献检查

## 0.3.1 - 2026-08-17

- 新增 `--clip`：从系统剪贴板读取（适合从 Word 复制后直接检查）

## 0.3.0 - 2026-08-13

标点风格的处理方式有破坏性变更，详见下文第一条。

- **变更**：不再默认把逗号、分号、冒号、圆括号从全角改为半角。GB/T 7714—2025 第 6.2 条并未规定这些符号的全角/半角形式，实践中存在多种约定，擅自改写可能反而违背用户所在期刊或学校的要求。需要统一半角时使用 `--punct half`。截断著者列表时改为复用条目原有的分隔符
- **修复**：机构作者不再被破坏——`WHO`、`UNESCO`、`IEEE` 等缩写以及"整段著者仅为一个全大写词"的情形都会保持原样，此前会被改成 `Who`、`Unesco`
- **修复**：`SMITH J III` 的世代后缀和罗马数字不再被改成 `Iii`
- **修复**：URL 内的全角句点不再被报告为"可自动修复"——修复函数为保护 URL 会跳过它，导致该警告永远无法消除。检查与修复现已共用同一套 URL 掩码
- **修复**：`Smith J.A., Brown K` 这类带点缩写名不再被截断，著者人数统计随之正确
- **修复**：`[ds/ol]` 的大小写问题此前报告为 `[ds] → [DS]`，用户在文档中搜不到该字符串；现合并为 `[ds/ol] → [DS/OL]`
- **修复**：序号重复不再同时被报告为"序号不连续"
- **修复**：`[2024.2.30]` 等不存在的日期不再被"规范化"
- **修复**：`--fix` 重定向到文件时按 UTF-8 写出，避免在 GBK 控制台下丢字符
- 新增 W112：著者之间使用顿号"、"分隔（标准第 6.2 条规定责任者之间用逗号）。题名中的顿号不受影响；此前这类条目的著者会被当作 1 人，导致 W105 漏报
- 新增：`--ignore` / `--select` 按规则过滤，`--punct` 选择标点风格
- 新增：识别"参考文献""References"等标题行，不再对其报告缺少文献类型标识，`--fix` 时原样保留；空行布局也一并保留
- 新增：修复流程迭代至收敛，并新增不变量测试——在真实脏数据语料和两种标点风格下断言修复幂等、`--fix` 后不残留可修复项、每条规则的建议与其自身修复结果一致

## 0.2.0 - 2026-08-13

- 修复：日期规范化不再把圆括号"(更新/发布日期)"错改成方括号——两种括号是不同著录项，现在分别识别、分别保留
- 修复：W103 删除引用日期时不再误删圆括号日期
- 新增 W110：中文文献误用"et al."/外文文献误用"等"，双向自动修复
- 新增 W111：文献类型标识前的多余空格（"标题 [J]"→"标题[J]"），自动修复
- 改进：外文著者分隔符修复为半角逗号加空格（", "），中文著者为半角逗号（","）
- 改进：CLI 在非 UTF-8 控制台（如 cp437）上不再因中文消息崩溃

## 0.1.0 - 2026-08-13

首个版本。

- 参考文献纯文本解析：支持 `[1]`、`1.`、`（1）`、`【1】` 等编号形式与跨行条目
- 11 条检查规则（E001–E002，W101–W109），覆盖 GB/T 7714—2025 相对 2015 版的主要变化：
  - 外文姓氏改为首字母大写（拼音姓氏保留全大写）
  - 其他责任者"等，译"新增逗号
  - 非在线资源不著录引用日期
  - 文献类型标识改为必备项，新增 PP / DS / A 类型与 MM 载体
- `--fix` 自动修复 7 类机械性问题，URL 内容保护
- `--json` 机器可读输出；错误存在时退出码为 1
- 输入编码自动识别（UTF-8 / GBK）
